import FirebaseAuth from "./firebaseAuth";

const Login = () => {
    return (
      <div>
      {/* Render the FirebaseAuth component */}
      <FirebaseAuth />
    </div>
    )
}

export default Login